NODE_ENV=development
MONGO_URI=YOUR MONGO URL
PORT=YOUR PORT NUMBER
ACCESS_TOKEN_SECRET=YOUR ACCESS TOKEN SCRETE
REFRESH_TOKEN_SECRET=YOUR REFRESH TOKEN SCRETE
CLIENT_URL=YOUR FRONTEND URL
QR_JWT_SECRET=YOUR QR GENERATION SCRETE

# Optional — if unset, emails are logged to console instead of sent
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@fitnessaggregator.com.au